name: Integrate CRM1 Advanced Layer

on:
  push:
    paths:
      - 'crm1/advanced-business-layer.js'
      - '.github/workflows/integrate-crm1-advanced.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Inject cache-busted advanced layer loader without replacing CRM1
        shell: python
        run: |
          from pathlib import Path
          p = Path('crm1/index.html')
          text = p.read_text(encoding='utf-8')
          old = '<script src="./advanced-business-layer.js"></script>'
          new = '<script src="./advanced-business-layer.js?v=2"></script>'
          if new in text:
              print('Advanced layer loader already cache-busted.')
          elif old in text:
              text = text.replace(old, new, 1)
              p.write_text(text, encoding='utf-8')
              print('Updated advanced layer loader to cache-busted v2.')
          else:
              marker = '</body>'
              if marker not in text:
                  raise SystemExit('CRM1 index.html does not contain </body>; refusing unsafe modification')
              text = text.replace(marker, new + '\n' + marker, 1)
              p.write_text(text, encoding='utf-8')
              print('Injected cache-busted advanced layer loader.')

      - name: Commit integration if changed
        shell: bash
        run: |
          if git diff --quiet -- crm1/index.html; then
            echo 'No CRM1 integration change required.'
            exit 0
          fi
          git config user.name 'Aaroogyam CRM Bot'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add crm1/index.html
          git commit -m 'Cache-bust CRM1 advanced module bootstrap [skip ci]'
          git push
