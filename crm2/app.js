import {sb,esc,fmtDate,money,normalizeMobile,parseCsv,currentProfile,getSources,getProducts,searchLeads,createLead,logCall,createFollowup,completeFollowup,updateOrderStatus,updateVerification,softDeleteLead} from './data.js';
import {createOrderFromLead,changeShipmentStatus,createNdrCase,createRtoCase,recordPayment} from './modules/workflows.js';
const root=document.querySelector('#root');let me=null,page='dashboard';
const $=id=>document.getElementById(id),fail=e=>{console.error(e);return e?.message||'Something went wrong.'},canManage=()=>!['agent','management_readonly'].includes(me?.role);
const nav=[['dashboard','Dashboard'],['leads','Leads'],['customers','Customers'],['calling','Calling'],['followups','Follow-ups'],['orders','Orders'],['verification','Verification'],['inventory','Inventory'],['dealers','Dealers'],['delivery','Delivery / NDR / RTO'],['accounts','Accounts'],['reports','MIS & Analytics'],['targets','Agent Targets'],['imports','Import Leads'],['audit','Audit Log']];
function shell(){root.innerHTML=`<div class="shell"><header class="top"><strong>Aaroogyam CRM2</strong><div class="user">${esc(me.full_name||me.email)} · ${esc(me.role)} <button id="logout" class="btn alt">Logout</button></div></header><div class="layout"><aside class="side">${nav.map(([id,n])=>`<button data-page="${id}" class="${id===page?'active':''}">${n}</button>`).join('')}</aside><main class="main" id="main"></main></div></div>`;document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{page=b.dataset.page;render()});$('logout').onclick=async()=>{await sb.auth.signOut();boot()}}
async function render(){shell();const m=$('main');m.innerHTML='<div class="panel">Loading…</div>';try{await ({dashboard,leads,customers,calling,followups,orders,verification,inventory,dealers,delivery,accounts,reports,targets,imports,audit}[page])(m)}catch(e){m.innerHTML=`<section class="panel error">${esc(fail(e))}</section>`}}
async function boot(){const p=await currentProfile();if(!p){await sb.auth.signOut();login('Please use an active CRM2 account.');return}me=p;render()}
function login(msg=''){root.innerHTML=`<main class="login"><section class="login-card"><div class="brand"><h1>Aaroogyam CRM2</h1><p>Zero-cost Teleshopping Business ERP</p></div><form id="loginForm"><div class="field"><label>Email</label><input id="email" type="email" autocomplete="username" required></div><div class="field"><label>Password</label><input id="password" type="password" autocomplete="current-password" required></div><button class="btn">Login</button><div class="msg">${esc(msg)}</div></form></section></main>`;$('loginForm').onsubmit=async e=>{e.preventDefault();const {error}=await sb.auth.signInWithPassword({email:$('email').value.trim(),password:$('password').value});if(error)login(error.message);else boot()}}
const card=(label,value)=>`<div class="stat"><span>${esc(label)}</span><b>${esc(value)}</b></div>`,table=(h,r,e='No records found.')=>`<div class="tablewrap"><table><thead><tr>${h.map(x=>`<th>${x}</th>`).join('')}</tr></thead><tbody>${r.length?r.join(''):`<tr><td colspan="${h.length}" class="muted">${e}</td></tr>`}</tbody></table></div>`;
async function counts(){const qs=await Promise.all([sb.from('leads').select('id',{count:'exact',head:true}).is('deleted_at',null),sb.from('orders').select('id',{count:'exact',head:true}),sb.from('followups').select('id',{count:'exact',head:true}).eq('status','pending'),sb.from('shipments').select('id',{count:'exact',head:true}).eq('status','delivered'),sb.from('shipments').select('id',{count:'exact',head:true}).eq('status','rto')]);return qs.map(x=>x.count||0)}
async function dashboard(m){const [l,o,f,d,r]=await counts();m.innerHTML=`<div class="title"><h2>Management Dashboard</h2><span class="muted">${new Date().toLocaleDateString('en-IN')}</span></div><div class="cards">${card('Active Leads',l)}${card('Orders',o)}${card('Pending Follow-ups',f)}${card('Delivered',d)}${card('RTO',r)}${card('Role',me.role)}</div><section class="panel"><h3>Business Funnel</h3><div class="funnel"><span>Leads ${l}</span><span>→</span><span>Orders ${o}</span><span>→</span><span>Delivered ${d}</span><span>/ RTO ${r}</span></div></section>`}
async function leads(m,term=''){const {data,error}=await searchLeads(term);const rows=(data||[]).map(x=>`<tr><td><button class="link" data-lead="${x.id}">${esc(x.lead_code)}</button></td><td>${esc(x.customer_name)}</td><td>${esc(x.mobile)}</td><td>${esc(x.product_name)}</td><td>${esc(x.source)}</td><td><span class="pill">${esc(x.status)}</span></td><td>${esc(x.priority)}</td><td>${fmtDate(x.next_followup)}</td><td>${canManage()?`<button class="btn alt" data-delete="${x.id}">Delete</button>`:''}</td></tr>`);m.innerHTML=`<div class="title"><h2>Leads</h2><button class="btn" id="newLead">New Lead</button></div><section class="panel"><form id="leadSearch" class="toolbar"><input id="q" value="${esc(term)}" placeholder="Mobile / name / lead ID"><button class="btn alt">Search</button></form>${error?`<p class="error">${esc(error.message)}</p>`:table(['Lead','Customer','Mobile','Product','Source','Status','Priority','Follow-up','Action'],rows)}</section>`;$('newLead').onclick=()=>leadForm(m);$('leadSearch').onsubmit=e=>{e.preventDefault();leads(m,$('q').value.trim())};document.querySelectorAll('[data-lead]').forEach(b=>b.onclick=()=>leadDetail(m,b.dataset.lead));document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{const reason=prompt('Reason for soft delete?');if(!reason)return;const {error}=await softDeleteLead(b.dataset.delete,reason);if(error)alert(error.message);else leads(m,term)})}
async function leadForm(m){const {data:sources}=await getSources();m.innerHTML=`<section class="panel"><h2>New Lead</h2><form id="leadForm" class="grid2"><div class="field"><label>Customer Name</label><input id="name" required></div><div class="field"><label>Mobile</label><input id="mobile" inputmode="numeric" pattern="[0-9]{10}" required></div><div class="field"><label>Alternate Mobile</label><input id="alt"></div><div class="field"><label>Product Interest</label><input id="product"></div><div class="field"><label>Source</label><select id="source">${(sources||[]).map(x=>`<option>${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>City</label><input id="city"></div><div class="field"><label>State</label><input id="state"></div><div class="field"><label>Pincode</label><input id="pin"></div><div class="field full"><label>Address</label><textarea id="address"></textarea></div><div class="field"><label>Priority</label><select id="priority"><option>normal</option><option>high</option><option>urgent</option><option>express</option></select></div><button class="btn">Save Lead</button><div id="formMsg" class="msg"></div></form></section>`;$('leadForm').onsubmit=async e=>{e.preventDefault();try{await createLead({customer_name:$('name').value.trim(),mobile:normalizeMobile($('mobile').value),alternate_mobile:normalizeMobile($('alt').value),product_name:$('product').value.trim(),source:$('source').value,city:$('city').value.trim(),state:$('state').value.trim(),pincode:$('pin').value.trim(),address:$('address').value.trim(),priority:$('priority').value});leads(m)}catch(x){$('formMsg').textContent=fail(x)}}}
async function leadDetail(m,id){const {data:l,error}=await sb.from('leads').select('*').eq('id',id).single();if(error){m.innerHTML=`<section class="panel error">${esc(error.message)}</section>`;return}const [{data:calls},{data:fu}]=await Promise.all([sb.from('lead_calls').select('call_source,direction,outcome,notes,duration_seconds,created_at').eq('lead_id',id).order('created_at',{ascending:false}).limit(30),sb.from('followups').select('id,due_at,reason,status,priority').eq('lead_id',id).order('due_at').limit(30)]);m.innerHTML=`<div class="title"><h2>${esc(l.lead_code)} · ${esc(l.customer_name)}</h2><button class="btn alt" id="back">Back</button></div><section class="panel"><div class="grid2"><p><b>Mobile</b><br>${esc(l.mobile)}<br><a class="btn" href="tel:${encodeURIComponent(l.mobile)}">Call Mobile</a></p><p><b>Status</b><br><span class="pill">${esc(l.status)}</span></p><p><b>Product</b><br>${esc(l.product_name)}</p><p><b>Address</b><br>${esc([l.address,l.city,l.state,l.pincode].filter(Boolean).join(', '))}</p></div></section><section class="panel"><h3>Call History</h3>${table(['Source','Direction','Outcome','Duration','Notes','Time'],(calls||[]).map(x=>`<tr><td>${esc(x.call_source)}</td><td>${esc(x.direction)}</td><td>${esc(x.outcome)}</td><td>${x.duration_seconds}s</td><td>${esc(x.notes)}</td><td>${fmtDate(x.created_at)}</td></tr>`))}</section><section class="panel"><h3>Follow-ups</h3>${table(['Due','Priority','Reason','Status','Action'],(fu||[]).map(x=>`<tr><td>${fmtDate(x.due_at)}</td><td>${esc(x.priority)}</td><td>${esc(x.reason)}</td><td>${esc(x.status)}</td><td>${x.status==='pending'?`<button class="btn alt" data-complete="${x.id}">Complete</button>`:''}</td></tr>`))}<button class="btn" id="addFollow">Add Follow-up</button></section>`;$('back').onclick=()=>leads(m);$('addFollow').onclick=()=>followupForm(m,id,l.customer_id);document.querySelectorAll('[data-complete]').forEach(b=>b.onclick=async()=>{await completeFollowup(b.dataset.complete);leadDetail(m,id)})}
async function customers(m){let q=prompt('Search customer mobile/name (leave blank for latest)')||'';let query=sb.from('customers').select('id,customer_code,name,mobile,city,state,pincode,created_at').order('created_at',{ascending:false}).limit(100);if(q){const t=q.replace(/[%(),]/g,'');query=query.or(`mobile.ilike.%${t}%,name.ilike.%${t}%,customer_code.ilike.%${t}%`)}const {data,error}=await query;const rows=(data||[]).map(x=>`<tr><td><button class="link" data-customer="${x.id}">${esc(x.customer_code)}</button></td><td>${esc(x.name)}</td><td>${esc(x.mobile)}</td><td>${esc(x.city)}</td><td>${esc(x.state)}</td><td>${fmtDate(x.created_at)}</td></tr>`);m.innerHTML=`<div class="title"><h2>Customer 360</h2></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Code','Name','Mobile','City','State','Created'],rows)}</section>`;document.querySelectorAll('[data-customer]').forEach(b=>b.onclick=()=>customer360(m,b.dataset.customer))}
async function customer360(m,id){const [{data:c},{data:a},{data:o},{data:n}]=await Promise.all([sb.from('customers').select('*').eq('id',id).single(),sb.from('customer_addresses').select('*').eq('customer_id',id),sb.from('orders').select('order_code,total,status,payment_mode,created_at').eq('customer_id',id).order('created_at',{ascending:false}),sb.from('customer_notes').select('note,created_at').eq('customer_id',id).order('created_at',{ascending:false})]);m.innerHTML=`<div class="title"><h2>${esc(c?.name)} · Customer 360</h2><button class="btn alt" id="back">Back</button></div><section class="panel"><div class="grid2"><p><b>Mobile</b><br>${esc(c?.mobile)}</p><p><b>Alternate</b><br>${esc(c?.alternate_mobile)}</p><p><b>Location</b><br>${esc([c?.city,c?.state,c?.pincode].filter(Boolean).join(', '))}</p></div></section><section class="panel"><h3>Orders</h3>${table(['Order','Total','Payment','Status','Date'],(o||[]).map(x=>`<tr><td>${esc(x.order_code)}</td><td>${money(x.total)}</td><td>${esc(x.payment_mode)}</td><td>${esc(x.status)}</td><td>${fmtDate(x.created_at)}</td></tr>`))}</section><section class="panel"><h3>Addresses</h3>${table(['Label','Address','City','State','Pincode'],(a||[]).map(x=>`<tr><td>${esc(x.label)}</td><td>${esc(x.address)}</td><td>${esc(x.city)}</td><td>${esc(x.state)}</td><td>${esc(x.pincode)}</td></tr>`))}</section><section class="panel"><h3>Notes</h3>${(n||[]).map(x=>`<p>${esc(x.note)} <span class="muted">${fmtDate(x.created_at)}</span></p>`).join('')||'<p class="muted">No notes.</p>'}</section>`;$('back').onclick=()=>customers(m)}
async function calling(m){const {data,error}=await sb.from('leads').select('id,customer_name,mobile,product_name,status').is('deleted_at',null).in('status',['new','assigned','callback','working']).order('next_followup',{nullsFirst:true}).limit(100);const rows=(data||[]).map(x=>`<tr><td>${esc(x.customer_name)}</td><td>${esc(x.mobile)}</td><td>${esc(x.product_name)}</td><td>${esc(x.status)}</td><td><a class="btn" href="tel:${encodeURIComponent(x.mobile)}">Call</a> <button class="btn alt" data-log="${x.id}">Log Outcome</button></td></tr>`);m.innerHTML=`<div class="title"><h2>Calling Workspace</h2></div><section class="panel"><p class="muted">Manual Mobile calling is always available. Dialer integration stays behind a backend adapter.</p>${error?`<p class="error">${esc(error.message)}</p>`:table(['Customer','Mobile','Product','Status','Action'],rows)}</section>`;document.querySelectorAll('[data-log]').forEach(b=>b.onclick=()=>callForm(m,b.dataset.log))}
async function callForm(m,id){const {data:l}=await sb.from('leads').select('id,customer_id,customer_name,mobile').eq('id',id).single();const {data:disp}=await sb.from('disposition_levels').select('id,name,level').eq('active',true).order('level').order('name');m.innerHTML=`<section class="panel"><h2>Log Call · ${esc(l.customer_name)}</h2><p><a class="btn" href="tel:${encodeURIComponent(l.mobile)}">Call ${esc(l.mobile)}</a></p><form id="callForm" class="grid2"><div class="field"><label>Outcome</label><select id="outcome"><option>Connected</option><option>No Response</option><option>Call Drop</option><option>Not Reachable</option></select></div><div class="field"><label>Disposition</label><select id="disp"><option value="">Select</option>${(disp||[]).map(x=>`<option value="${x.id}">${esc(x.name)} (L${x.level})</option>`).join('')}</select></div><div class="field"><label>Duration (seconds)</label><input id="duration" type="number" min="0" value="0"></div><div class="field full"><label>Notes</label><textarea id="notes"></textarea></div><button class="btn">Save Call</button><div id="msg" class="msg"></div></form></section>`;$('callForm').onsubmit=async e=>{e.preventDefault();try{await logCall({lead:l,agentId:me.id,outcome:$('outcome').value,dispositionId:$('disp').value||null,notes:$('notes').value.trim(),duration:$('duration').value});calling(m)}catch(x){$('msg').textContent=fail(x)}}}
async function followups(m){const {data,error}=await sb.from('followups').select('id,lead_id,customer_id,order_id,assigned_to,due_at,priority,reason,status').order('due_at').limit(200);const now=Date.now(),pending=(data||[]).filter(x=>x.status==='pending'),overdue=pending.filter(x=>new Date(x.due_at).getTime()<now);m.innerHTML=`<div class="title"><h2>Follow-ups</h2><button class="btn" id="newFollow">New Follow-up</button></div><div class="cards">${card('Pending',pending.length)}${card('Overdue',overdue.length)}${card('Completed',(data||[]).filter(x=>x.status==='completed').length)}</div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Lead / Order / Customer','Due','Priority','Reason','Status','Action'],(data||[]).map(x=>`<tr><td>${esc(x.lead_id||x.order_id||x.customer_id||'—')}</td><td>${fmtDate(x.due_at)}</td><td>${esc(x.priority)}</td><td>${esc(x.reason)}</td><td>${esc(x.status)}${x.status==='pending'&&new Date(x.due_at)<new Date()?' · OVERDUE':''}</td><td>${x.status==='pending'?`<button class="btn alt" data-complete="${x.id}">Complete</button>`:''}</td></tr>`))}</section>`;$('newFollow').onclick=()=>followupForm(m);document.querySelectorAll('[data-complete]').forEach(b=>b.onclick=async()=>{await completeFollowup(b.dataset.complete);followups(m)})}
function followupForm(m,leadId='',customerId=''){m.innerHTML=`<section class="panel"><h2>New Follow-up</h2><form id="fuForm" class="grid2"><div class="field"><label>Lead ID</label><input id="leadId" value="${esc(leadId)}"></div><div class="field"><label>Customer ID</label><input id="customerId" value="${esc(customerId)}"></div><div class="field"><label>Due</label><input id="due" type="datetime-local" required></div><div class="field"><label>Priority</label><select id="priority"><option>normal</option><option>high</option><option>urgent</option><option>express</option></select></div><div class="field full"><label>Reason</label><input id="reason" required></div><button class="btn">Save</button><div id="msg" class="msg"></div></form></section>`;$('fuForm').onsubmit=async e=>{e.preventDefault();const {error}=await createFollowup({lead_id:$('leadId').value||null,customer_id:$('customerId').value||null,assigned_to:me.id,due_at:new Date($('due').value).toISOString(),priority:$('priority').value,reason:$('reason').value,status:'pending',reminder:true});if(error)$('msg').textContent=error.message;else followups(m)}}
async function orders(m){const {data,error}=await sb.from('orders').select('id,order_code,customer_id,lead_id,total,discount,payment_mode,status,priority,courier,created_at').order('created_at',{ascending:false}).limit(200);const rows=(data||[]).map(x=>`<tr><td>${esc(x.order_code)}</td><td>${esc(x.customer_id)}</td><td>${money(x.total)}</td><td>${esc(x.payment_mode)}</td><td><span class="pill">${esc(x.status)}</span></td><td>${esc(x.priority)}</td><td>${esc(x.courier||'—')}</td><td>${fmtDate(x.created_at)}</td><td><button class="btn alt" data-status="${x.id}">Status</button></td></tr>`);m.innerHTML=`<div class="title"><h2>Orders</h2><button class="btn" id="fastOrder">Fast Order</button></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Order','Customer','Total','Payment','Status','Priority','Courier','Created','Action'],rows)}</section>`;$('fastOrder').onclick=()=>orderForm(m);document.querySelectorAll('[data-status]').forEach(b=>b.onclick=()=>statusForm(m,b.dataset.status))}
async function orderForm(m){const {data:p}=await getProducts();m.innerHTML=`<section class="panel"><h2>Fast Order</h2><form id="orderForm" class="grid2"><div class="field"><label>Customer ID</label><input id="customerId" required placeholder="Customer UUID"></div><div class="field"><label>Lead ID</label><input id="leadId"></div><div class="field"><label>Product</label><select id="productId" required>${(p||[]).map(x=>`<option value="${x.id}" data-price="${x.selling_price}">${esc(x.name)} · ${money(x.selling_price)}</option>`).join('')}</select></div><div class="field"><label>Quantity</label><input id="qty" type="number" min="1" value="1" required></div><div class="field"><label>Unit Price</label><input id="price" type="number" min="0" step="0.01" required></div><div class="field"><label>Discount</label><input id="discount" type="number" min="0" step="0.01" value="0"></div><div class="field"><label>Payment</label><select id="payment"><option>COD</option><option>Prepaid</option></select></div><div class="field"><label>Priority</label><select id="priority"><option>normal</option><option>high</option><option>urgent</option><option>express</option></select></div><div class="field full"><label>Remarks</label><textarea id="remarks"></textarea></div><button class="btn">Create Order</button><div id="msg" class="msg"></div></form></section>`;const setPrice=()=>{$('price').value=$('productId').selectedOptions[0]?.dataset.price||0};setPrice();$('productId').onchange=setPrice;$('orderForm').onsubmit=async e=>{e.preventDefault();try{const {data:o,error}=await sb.from('orders').insert({customer_id:$('customerId').value,lead_id:$('leadId').value||null,agent_id:me.id,total:Math.max(0,Number($('qty').value)*Number($('price').value)-Number($('discount').value)),discount:Number($('discount').value)||0,payment_mode:$('payment').value,source:'CRM2',remarks:$('remarks').value,priority:$('priority').value,status:'new'}).select().single();if(error)throw error;const p=$('productId').selectedOptions[0];const {error:e2}=await sb.from('order_items').insert({order_id:o.id,product_id:$('productId').value,product_name:p.textContent.split(' · ')[0],quantity:Number($('qty').value),unit_price:Number($('price').value),line_total:Number($('qty').value)*Number($('price').value)});if(e2)throw e2;await sb.from('order_status_history').insert({order_id:o.id,status:'new',remarks:'Created from fast order'});orders(m)}catch(x){$('msg').textContent=fail(x)}}}
async function statusForm(m,id){const statuses=['new','confirmed','verification_pending','verified','rejected','packed','assigned','dispatched','in_transit','out_for_delivery','delivered','ndr','rto','cancelled','returned','refunded'];m.innerHTML=`<section class="panel"><h2>Update Order Status</h2><form id="sf"><div class="field"><label>Status</label><select id="status">${statuses.map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field"><label>Remarks</label><textarea id="remarks"></textarea></div><button class="btn">Save</button></form></section>`;$('sf').onsubmit=async e=>{e.preventDefault();try{await updateOrderStatus(id,$('status').value,$('remarks').value);orders(m)}catch(x){alert(fail(x))}}}
async function verification(m){const {data,error}=await sb.from('order_verifications').select('id,order_id,status,remarks,verified_at').order('created_at',{ascending:false}).limit(200);m.innerHTML=`<div class="title"><h2>Order Verification</h2></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Order','Status','Remarks','Verified','Action'],(data||[]).map(x=>`<tr><td>${esc(x.order_id)}</td><td>${esc(x.status)}</td><td>${esc(x.remarks)}</td><td>${fmtDate(x.verified_at)}</td><td>${canManage()||me.role==='verification'?`<button class="btn alt" data-verify="${x.id}">Update</button>`:''}</td></tr>`))}</section>`;document.querySelectorAll('[data-verify]').forEach(b=>b.onclick=()=>verifyForm(m,b.dataset.verify))}
function verifyForm(m,id){m.innerHTML=`<section class="panel"><h2>Verification Decision</h2><form id="vf"><div class="field"><label>Status</label><select id="status"><option>Verified</option><option>Rejected</option><option>Callback Required</option><option>Wrong Number</option><option>Duplicate</option><option>Suspected Fraud</option></select></div><div class="field"><label>Remarks</label><textarea id="remarks" required></textarea></div><button class="btn">Save</button></form></section>`;$('vf').onsubmit=async e=>{e.preventDefault();try{await updateVerification(id,$('status').value,$('remarks').value);verification(m)}catch(x){alert(fail(x))}}}
async function inventory(m){const {data,error}=await sb.from('inventory').select('id,sku,warehouse_id,on_hand,reserved,damaged,updated_at').order('sku').limit(200);m.innerHTML=`<div class="title"><h2>Inventory / Warehouse</h2></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['SKU','Warehouse','On Hand','Reserved','Damaged','Updated'],(data||[]).map(x=>`<tr><td>${esc(x.sku)}</td><td>${esc(x.warehouse_id)}</td><td>${x.on_hand}</td><td>${x.reserved}</td><td>${x.damaged}</td><td>${fmtDate(x.updated_at)}</td></tr>`))}</section>`}
async function dealers(m){const {data,error}=await sb.from('dealers').select('code,name,type,mobile,city,state,territory,status,commission_pct').order('name').limit(200);m.innerHTML=`<div class="title"><h2>Dealer / Distributor</h2></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Code','Name','Type','Mobile','Location','Territory','Status','Commission'],(data||[]).map(x=>`<tr><td>${esc(x.code)}</td><td>${esc(x.name)}</td><td>${esc(x.type)}</td><td>${esc(x.mobile)}</td><td>${esc(x.city)}, ${esc(x.state)}</td><td>${esc(x.territory)}</td><td>${esc(x.status)}</td><td>${x.commission_pct}%</td></tr>`))}</section>`}
async function delivery(m){const {data,error}=await sb.from('shipments').select('id,order_id,courier,tracking_no,status,expected_delivery,updated_at').order('updated_at',{ascending:false}).limit(200);m.innerHTML=`<div class="title"><h2>Delivery / NDR / RTO</h2></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Order','Courier','Tracking','Status','Expected','Updated','Action'],(data||[]).map(x=>`<tr><td>${esc(x.order_id)}</td><td>${esc(x.courier||'—')}</td><td>${esc(x.tracking_no||'—')}</td><td>${esc(x.status)}</td><td>${fmtDate(x.expected_delivery)}</td><td>${fmtDate(x.updated_at)}</td><td><button class="btn alt" data-ship="${x.id}">Update</button></td></tr>`))}</section>`;document.querySelectorAll('[data-ship]').forEach(b=>b.onclick=()=>shipmentForm(m,b.dataset.ship))}
function shipmentForm(m,id){m.innerHTML=`<section class="panel"><h2>Shipment Update</h2><form id="shipForm"><div class="field"><label>Status</label><select id="status"><option>in_transit</option><option>out_for_delivery</option><option>delivered</option><option>ndr</option><option>rto</option></select></div><div class="field"><label>Remarks</label><textarea id="remarks"></textarea></div><button class="btn">Save</button></form></section>`;$('shipForm').onsubmit=async e=>{e.preventDefault();try{await changeShipmentStatus(id,$('status').value,$('remarks').value);delivery(m)}catch(x){alert(fail(x))}}}
async function accounts(m){const {data,error}=await sb.from('payments').select('order_id,amount,type,status,reference,created_at').order('created_at',{ascending:false}).limit(200);m.innerHTML=`<div class="title"><h2>Accounts / Collections</h2><button class="btn" id="pay">Record Payment</button></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Order','Amount','Type','Status','Reference','Date'],(data||[]).map(x=>`<tr><td>${esc(x.order_id)}</td><td>${money(x.amount)}</td><td>${esc(x.type)}</td><td>${esc(x.status)}</td><td>${esc(x.reference)}</td><td>${fmtDate(x.created_at)}</td></tr>`))}</section>`;$('pay').onclick=()=>paymentForm(m)}
function paymentForm(m){m.innerHTML=`<section class="panel"><h2>Record Payment</h2><form id="pf" class="grid2"><div class="field"><label>Order ID</label><input id="orderId" required></div><div class="field"><label>Amount</label><input id="amount" type="number" min="0" required></div><div class="field"><label>Mode</label><input id="mode" value="COD"></div><div class="field"><label>Reference</label><input id="ref"></div><button class="btn">Save</button></form></section>`;$('pf').onsubmit=async e=>{e.preventDefault();try{await recordPayment({orderId:$('orderId').value,amount:$('amount').value,mode:$('mode').value,reference:$('ref').value});accounts(m)}catch(x){alert(fail(x))}}}
async function reports(m){const [l,o,s]=await Promise.all([sb.from('leads').select('id,status,source,created_at').is('deleted_at',null).limit(1000),sb.from('orders').select('id,status,total,created_at').limit(1000),sb.from('shipments').select('id,status').limit(1000)]);const leadsN=l.data?.length||0,ordersN=o.data?.length||0,del=(s.data||[]).filter(x=>x.status==='delivered').length,rto=(s.data||[]).filter(x=>x.status==='rto').length;const revenue=(o.data||[]).reduce((a,x)=>a+Number(x.total||0),0);m.innerHTML=`<div class="title"><h2>MIS & Analytics</h2></div><div class="cards">${card('Leads',leadsN)}${card('Orders',ordersN)}${card('Conversion %',leadsN?((ordersN/leadsN)*100).toFixed(1)+'%':'0%')}${card('Delivered',del)}${card('RTO',rto)}${card('Order Value',money(revenue))}</div><section class="panel"><h3>Lead Source Summary</h3>${table(['Source','Leads'],Object.entries((l.data||[]).reduce((a,x)=>(a[x.source]=(a[x.source]||0)+1,a),{})).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<tr><td>${esc(k)}</td><td>${v}</td></tr>`))}</section>`}
async function imports(m){m.innerHTML=`<div class="title"><h2>Lead Import</h2></div><section class="panel"><p class="muted">CSV/XLSX: local preview → mapping → validation → duplicate check → staging → import.</p><input id="file" type="file" accept=".csv,.xlsx,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"><div id="importArea"></div></section>`;$('file').onchange=()=>prepareImport(m,$('file').files[0])}
async function prepareImport(m,file){if(!file)return;let rows;if(file.name.toLowerCase().endsWith('.xlsx')){try{const XLSX=await import('https://cdn.jsdelivr.net/npm/xlsx@0.18.5/+esm');const wb=XLSX.read(await file.arrayBuffer());rows=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{header:1,defval:''})}catch(e){$('importArea').innerHTML=`<p class="error">Excel parser unavailable: ${esc(fail(e))}</p>`;return}}else rows=parseCsv(await file.text());if(!rows?.length){$('importArea').innerHTML='<p class="error">Empty file.</p>';return}const headers=rows[0].map((x,i)=>String(x||`Column ${i+1}`).trim()),fields=[['mobile','Mobile*'],['customer_name','Customer Name'],['alternate_mobile','Alternate Mobile'],['product_name','Product Interest'],['source','Source'],['city','City'],['state','State'],['pincode','Pincode'],['address','Address']];$('importArea').innerHTML=`<h3>1. Column Mapping</h3><form id="mapForm" class="grid2">${fields.map(([f,label])=>`<div class="field"><label>${label}</label><select data-map="${f}"><option value="">Not mapped</option>${headers.map((h,i)=>`<option value="${i}" ${/mobile|phone|contact/i.test(h)&&f==='mobile'?'selected':''}>${esc(h)}</option>`).join('')}</select></div>`).join('')}<button class="btn">Validate & Stage</button></form><h3>Preview</h3>${table(headers,(rows.slice(1,6)).map(r=>`<tr>${headers.map((_,i)=>`<td>${esc(r[i])}</td>`).join('')}</tr>`))}<div id="stage"></div>`;$('mapForm').onsubmit=async e=>{e.preventDefault();const map={};document.querySelectorAll('[data-map]').forEach(s=>map[s.dataset.map]=s.value===''?null:Number(s.value));await stageImport(file,rows,map)}}
async function stageImport(file,rows,map){const raw=rows.slice(1),staged=[];let invalid=0,dupes=0;for(const r of raw){const o={};Object.entries(map).forEach(([f,i])=>{if(i!==null)o[f]=String(r[i]??'').trim()});o.mobile=normalizeMobile(o.mobile);let bad=o.mobile.length!==10?'Invalid mobile':'';if(!bad){const {data:d}=await sb.from('leads').select('id').eq('mobile',o.mobile).is('deleted_at',null).limit(1);if(d?.length){bad='Duplicate active mobile';dupes++}}if(bad)invalid++;else staged.push(o)}const {data:imp,error}=await sb.from('imports').insert({file_name:file.name,source:'CSV/Excel',total_rows:raw.length,valid_rows:staged.length,duplicate_rows:dupes,invalid_rows:invalid,imported_rows:0,skipped_rows:invalid,status:'validated',column_mapping:map,validation_summary:{valid:staged.length,invalid,duplicates:dupes},created_by:me.id}).select().single();if(error){$('stage').innerHTML=`<p class="error">${esc(error.message)}</p>`;return}for(let i=0;i<raw.length;i+=500){const payload=raw.slice(i,i+500).map((r,j)=>{const o={};Object.entries(map).forEach(([f,k])=>{if(k!==null)o[f]=String(r[k]??'').trim()});const mobile=normalizeMobile(o.mobile);return {import_id:imp.id,row_no:i+j+2,raw_data:o,status:mobile.length===10?'validated':'invalid',error_message:mobile.length===10?null:'Invalid mobile'}});await sb.from('import_rows').insert(payload)}$('stage').innerHTML=`<div class="panel"><div class="cards">${card('Total',raw.length)}${card('Valid',staged.length)}${card('Duplicates',dupes)}${card('Invalid',invalid)}</div><button class="btn" id="commitImport">Import Valid Leads</button></div>`;$('commitImport').onclick=async()=>commitImport(imp.id,staged)}
async function commitImport(importId,staged){let imported=0,failed=0;for(let i=0;i<staged.length;i+=100){const {data,error}=await sb.from('leads').insert(staged.slice(i,i+100).map(x=>({...x,status:'new',priority:'normal',lead_date:new Date().toISOString()}))).select('id');if(error)failed+=Math.min(100,staged.length-i);else imported+=data?.length||0}await sb.from('imports').update({status:failed?'completed_with_errors':'completed',imported_rows:imported,skipped_rows:failed}).eq('id',importId);alert(`Import complete. Imported: ${imported}, failed: ${failed}`);imports($('main'))}
async function audit(m){const {data,error}=await sb.from('audit_logs').select('created_at,actor_id,action,entity_type,entity_id,metadata').order('created_at',{ascending:false}).limit(200);m.innerHTML=`<div class="title"><h2>Audit Log</h2></div><section class="panel">${error?`<p class="error">${esc(error.message)}</p>`:table(['Time','Actor','Action','Entity','ID','Metadata'],(data||[]).map(x=>`<tr><td>${fmtDate(x.created_at)}</td><td>${esc(x.actor_id)}</td><td>${esc(x.action)}</td><td>${esc(x.entity_type)}</td><td>${esc(x.entity_id)}</td><td><code>${esc(JSON.stringify(x.metadata||{}))}</code></td></tr>`))}</section>`}
boot();

async function targets(m){return window.crm2Targets?window.crm2Targets(m):m.innerHTML='<section class="panel">Loading targets…</section>'}
