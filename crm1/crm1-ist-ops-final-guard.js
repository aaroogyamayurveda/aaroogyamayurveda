/* CRM1 Operations final owner guard v1
 * Prevents the legacy inline Professional Modules renderer from overwriting
 * the IST-aware Operations renderers after async navigation/page loads.
 */
(function(){
  'use strict';
  if(window.__crm1OpsFinalGuardStarted)return;
  window.__crm1OpsFinalGuardStarted=true;
  var TZ='Asia/Kolkata', timers={};
  var $=function(id){return document.getElementById(id)};
  var esc=function(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(m){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]})};
  var money=function(v){return '₹'+Number(v||0).toLocaleString('en-IN')};
  var istDate=function(){return new Intl.DateTimeFormat('en-CA',{timeZone:TZ,year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())};
  function nextDate(s){var d=new Date(s+'T00:00:00+05:30');d.setUTCDate(d.getUTCDate()+1);return new Intl.DateTimeFormat('en-CA',{timeZone:TZ,year:'numeric',month:'2-digit',day:'2-digit'}).format(d)}
  function range(f,t){f=f||istDate();t=t||f;if(t<f){var z=f;f=t;t=z}return{from:new Date(f+'T00:00:00+05:30').toISOString(),to:new Date(nextDate(t)+'T00:00:00+05:30').toISOString(),fromDate:f,toDate:t}}
  function active(id){var e=$(id);return !!(e&&e.classList.contains('active'))}
  function mapPartners(){return Promise.all([window.sb.from('dealers').select('id,dealer_name,is_active'),window.sb.from('profiles').select('id,full_name,is_active').eq('role','courier_manager')])}
  function hide(c){if(c)c.style.visibility='hidden'}
  function show(c){if(c)c.style.visibility='visible'}
  function marker(c){return !!(c&&c.querySelector('[data-crm1-ops-final="1"]'))}
  async function renderPartner(){
    var p=$('partnerPerformance'),c=$('partnerPerformanceContent');if(!p||!c||!p.classList.contains('active')||!window.sb)return;
    hide(c);
    c.innerHTML='<div data-crm1-ops-final="1"><div class="panel"><div class="crm1-toolbar" style="display:flex;gap:10px;align-items:center;flex-wrap:wrap"><label>From <input type="date" id="opsFinalPPFrom"></label><label>To <input type="date" id="opsFinalPPTo"></label><button class="btn" id="opsFinalPPApply">Apply</button><button class="btn alt" id="opsFinalPPToday">Today</button><span id="opsFinalPPMsg" class="sub"></span></div></div><div id="opsFinalPPStats" class="cards"></div><div class="panel"><div class="tablewrap"><table><thead><tr><th>Rank</th><th>Delivery Partner</th><th>Type</th><th>Assigned</th><th>In Progress</th><th>Delivered</th><th>RTO</th><th>Cancelled</th><th>Delivery %</th><th>Order Value</th><th>Revenue</th></tr></thead><tbody id="opsFinalPPBody"></tbody></table></div></div></div>';
    var t=istDate(),f=$('opsFinalPPFrom'),to=$('opsFinalPPTo');f.value=t;to.value=t;$('opsFinalPPApply').onclick=load;$('opsFinalPPToday').onclick=function(){f.value=istDate();to.value=istDate();load()};
    await load();
    async function load(){
      if(!active('partnerPerformance'))return;
      var msg=$('opsFinalPPMsg'),r=range(f.value||istDate(),to.value||f.value||istDate());if(!msg)return;msg.textContent='Loading…';
      try{var q=await Promise.all([window.sb.from('orders').select('id,order_no,order_status,total_amount,dealer_id,courier_manager_id,order_date,remarks').gte('order_date',r.from).lt('order_date',r.to),window.sb.from('dealers').select('id,dealer_name,is_active'),window.sb.from('profiles').select('id,full_name,is_active').eq('role','courier_manager')]);q.forEach(function(x){if(x.error)throw x.error});var o=q[0].data||[],dm=new Map((q[1].data||[]).map(function(x){return[x.id,x.dealer_name||x.id]})),cm=new Map((q[2].data||[]).map(function(x){return[x.id,x.full_name||x.id]})),m={};o.filter(function(x){return !String(x.remarks||'').includes('[ENQUIRY]')}).forEach(function(x){var type=x.dealer_id?'Dealer':'Courier',id=x.dealer_id||x.courier_manager_id;if(!id)return;var k=type+':'+id,y=m[k]||(m[k]={name:type==='Dealer'?dm.get(id):cm.get(id)||id,type:type,assigned:0,inprogress:0,delivered:0,rto:0,cancelled:0,value:0,revenue:0});y.assigned++;y.value+=Number(x.total_amount||0);if(x.order_status==='delivered'){y.delivered++;y.revenue+=Number(x.total_amount||0)}else if(x.order_status==='rto')y.rto++;else if(x.order_status==='cancelled')y.cancelled++;else y.inprogress++});var a=Object.values(m).sort(function(x,y){return y.revenue-x.revenue||y.delivered-x.delivered||y.assigned-x.assigned});$('opsFinalPPStats').innerHTML='<div class="stat"><span>Partners</span><b>'+a.length+'</b></div><div class="stat"><span>Assigned</span><b>'+a.reduce(function(s,x){return s+x.assigned},0)+'</b></div><div class="stat"><span>Delivered</span><b>'+a.reduce(function(s,x){return s+x.delivered},0)+'</b></div><div class="stat"><span>Revenue</span><b>'+money(a.reduce(function(s,x){return s+x.revenue},0))+'</b></div>';$('opsFinalPPBody').innerHTML=a.map(function(x,i){return '<tr><td><b>'+(i+1)+'</b></td><td><b>'+esc(x.name)+'</b></td><td>'+esc(x.type)+'</td><td>'+x.assigned+'</td><td>'+x.inprogress+'</td><td>'+x.delivered+'</td><td>'+x.rto+'</td><td>'+x.cancelled+'</td><td>'+(x.assigned?(x.delivered/x.assigned*100).toFixed(1):'0.0')+'%</td><td>'+money(x.value)+'</td><td><b>'+money(x.revenue)+'</b></td></tr>'}).join('')||'<tr><td colspan="11" class="empty">No delivery orders for selected period</td></tr>';msg.textContent='Report: '+r.fromDate+' to '+r.toDate;show(c)}catch(e){msg.textContent='Report error: '+(e.message||e);show(c)}}
  }
  async function renderSettlement(){
    var p=$('settlements'),c=$('settlementsContent');if(!p||!c||!p.classList.contains('active')||!window.sb)return;
    hide(c);c.innerHTML='<div data-crm1-ops-final="1"><div class="panel"><div class="crm1-toolbar" style="display:flex;gap:10px;align-items:center;flex-wrap:wrap"><label>From <input type="date" id="opsFinalSetFrom"></label><label>To <input type="date" id="opsFinalSetTo"></label><button class="btn" id="opsFinalSetApply">Apply</button><button class="btn alt" id="opsFinalSetToday">Today</button><span id="opsFinalSetMsg" class="sub"></span></div></div><div id="opsFinalSetKpis" class="cards"></div><div class="panel"><h3>Existing Settlements</h3><div class="tablewrap"><table><thead><tr><th>Partner</th><th>Period</th><th>Delivered</th><th>COD</th><th>Commission</th><th>Net Payable</th><th>Status</th></tr></thead><tbody id="opsFinalSetBody"></tbody></table></div></div><div class="panel"><h3>Delivered Orders — Pending Settlement</h3><div class="sub" style="margin-bottom:10px">Delivered orders for the selected IST date range that do not yet have a generated partner settlement record.</div><div class="tablewrap"><table><thead><tr><th>Order</th><th>Date / Time</th><th>Partner</th><th>Type</th><th>Customer</th><th>COD</th><th>Status</th></tr></thead><tbody id="opsFinalPending"></tbody></table></div></div></div>';
    var t=istDate(),f=$('opsFinalSetFrom'),to=$('opsFinalSetTo');f.value=t;to.value=t;$('opsFinalSetApply').onclick=load;$('opsFinalSetToday').onclick=function(){f.value=istDate();to.value=istDate();load()};await load();
    async function load(){
      if(!active('settlements'))return;var msg=$('opsFinalSetMsg'),r=range(f.value||istDate(),to.value||f.value||istDate());msg.textContent='Loading…';
      try{var mp=await mapPartners();if(mp[0].error)throw mp[0].error;if(mp[1].error)throw mp[1].error;var dm=new Map((mp[0].data||[]).map(function(x){return[x.id,x.dealer_name]})),cm=new Map((mp[1].data||[]).map(function(x){return[x.id,x.full_name]}));var s=await window.sb.from('partner_settlements').select('id,partner_id,period_from,period_to,delivered_orders,cod_amount,commission_amount,net_payable,status,partner_type').gte('period_from',r.fromDate).lte('period_to',r.toDate).order('period_from',{ascending:false});if(s.error)throw s.error;var rows=s.data||[];$('opsFinalSetBody').innerHTML=rows.map(function(x){var n=x.partner_type==='dealer'?dm.get(x.partner_id):cm.get(x.partner_id);return '<tr><td>'+esc(n||x.partner_id||'-')+'</td><td>'+x.period_from+' to '+x.period_to+'</td><td>'+Number(x.delivered_orders||0)+'</td><td>'+money(x.cod_amount)+'</td><td>'+money(x.commission_amount)+'</td><td>'+money(x.net_payable)+'</td><td>'+esc(x.status||'-')+'</td></tr>'}).join('')||'<tr><td colspan="7" class="empty">No settlements for selected period</td></tr>';var o=await window.sb.from('orders').select('id,order_no,order_status,total_amount,order_date,dealer_id,courier_manager_id,customers(customer_name),remarks').eq('order_status','delivered').gte('order_date',r.from).lt('order_date',r.to).or('remarks.is.null,remarks.not.ilike.%[ENQUIRY]%');if(o.error)throw o.error;var orders=o.data||[],existing=new Set();rows.forEach(function(x){existing.add(String(x.partner_id)+'|'+x.period_from+'|'+x.period_to)});var pending=orders.filter(function(x){return !existing.has(String(x.dealer_id||x.courier_manager_id)+'|'+r.fromDate+'|'+r.toDate)});$('opsFinalPending').innerHTML=pending.map(function(x){var n=x.dealer_id?dm.get(x.dealer_id):cm.get(x.courier_manager_id);return '<tr><td>#'+esc(x.order_no)+'</td><td>'+esc(new Intl.DateTimeFormat('en-IN',{timeZone:TZ,day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',hour12:true}).format(new Date(x.order_date)))+'</td><td>'+esc(n||'-')+'</td><td>'+esc(x.dealer_id?'Dealer':'Courier')+'</td><td>'+esc(x.customers&&x.customers.customer_name||'-')+'</td><td>'+money(x.total_amount)+'</td><td><span class="pill">delivered</span></td></tr>'}).join('')||'<tr><td colspan="7" class="empty">No delivered orders pending settlement</td></tr>';var cod=orders.reduce(function(s,x){return s+Number(x.total_amount||0)},0),comm=rows.reduce(function(s,x){return s+Number(x.commission_amount||0)},0),net=rows.reduce(function(s,x){return s+Number(x.net_payable||0)},0);$('opsFinalSetKpis').innerHTML='<div class="stat"><span>Settlements</span><b>'+rows.length+'</b></div><div class="stat"><span>Delivered</span><b>'+orders.length+'</b></div><div class="stat"><span>Pending Settlement Orders</span><b>'+pending.length+'</b></div><div class="stat"><span>Delivered COD</span><b>'+money(cod)+'</b></div><div class="stat"><span>Commission</span><b>'+money(comm)+'</b></div><div class="stat"><span>Net Settlement</span><b>'+money(net)+'</b></div>';msg.textContent='Report: '+r.fromDate+' to '+r.toDate;show(c)}catch(e){msg.textContent='Report error: '+(e.message||e);show(c)}}
  }
  function claim(id){var c=$(id+'Content');if(!c)return;hide(c);clearTimeout(timers[id]);timers[id]=setTimeout(function(){if(active(id)){if(id==='partnerPerformance')renderPartner();else if(id==='settlements')renderSettlement()}},80)}
  function init(){if(!document.body)return;var nav=$('nav');if(nav)nav.addEventListener('click',function(e){var b=e.target.closest&&e.target.closest('#nav button');if(!b)return;setTimeout(function(){if(active('partnerPerformance'))claim('partnerPerformance');if(active('settlements'))claim('settlements')},0)},true);var obs=new MutationObserver(function(muts){['partnerPerformance','settlements'].forEach(function(id){var p=$(id),c=$(id+'Content');if(!p||!c||!p.classList.contains('active'))return;if(!marker(c)){hide(c);clearTimeout(timers[id]);timers[id]=setTimeout(function(){if(active(id)){if(id==='partnerPerformance')renderPartner();else renderSettlement()},90)}}});});obs.observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});setTimeout(function(){if(active('partnerPerformance'))claim('partnerPerformance');if(active('settlements'))claim('settlements')},100)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();